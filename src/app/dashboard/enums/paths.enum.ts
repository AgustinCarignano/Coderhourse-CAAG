export enum Paths {
  AUTH = 'AUTH',
  LOGIN = 'login',
  REGISTER = 'register',
  DASHBOARD = 'dashboard',
  HOME = 'home',
  USERS = 'users',
  ALUMNS = 'alumns',
  COURSES = 'courses',
  DETAILS = 'details',
  ID = ':id',
}
