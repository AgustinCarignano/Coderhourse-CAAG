export enum Paths {
  AUTH = 'AUTH',
  LOGIN = 'login',
  REGISTER = 'register',
  DASHBOARD = 'dashboard',
  HOME = 'home',
  USERS = 'users',
  STUDENTS = 'students',
  COURSES = 'courses',
  DETAILS = 'details',
  ID = ':id',
}
